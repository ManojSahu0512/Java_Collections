package Java8Features.StreamAPI.Map;
/*What is map() in Stream API?
The map() method is used to transform each element of the stream.

It returns a new stream with the transformed elements.

Signature:
<R> Stream<R> map(Function<? super T, ? extends R> mapper);
*/
public class Syntax {
}
