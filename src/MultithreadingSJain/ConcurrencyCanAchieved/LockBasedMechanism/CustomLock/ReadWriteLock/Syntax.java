package MultithreadingSJain.ConcurrencyCanAchieved.LockBasedMechanism.CustomLock.ReadWriteLock;
/*
* ReadLock: More than 1 thread can acquire the read lock(shared lock)-->read only
* WriteLock: Only 1 thread can acquire the write lock.(exclusive lock)-->read write both
*
* If read is very high then using this lock*/
public class Syntax {
}
